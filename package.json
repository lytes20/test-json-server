{
  "name": "test-json-server",
  "version": "1.0.0",
  "description": "Test json server for projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gideon Bamuleseyo",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
